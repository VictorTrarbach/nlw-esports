// ISSO AQUI É A BASE DO REACT

//Toda interface pode ser construída só com esses 2 conceitos: componentes e propriedades

//Ainda sem vida, user manipulando, aparecer coisa nova clicando no botão etx...

{/* interface ButtonProps {
    title: string;
  }
  //Como acesso a propriedade title dentro do componente Button? 
  
  /*Como é uma função, basta receber as propriedades (props) do Componente 
  através do primeiro parâmetro desse botão*/
                         //Definido
  /*function Button(props: ButtonProps) {
    return(
        <button>
          {props.title}
        </button>
    )
  }
  function App() {
    return(
        <div>
            <Button title ="Send 1"/>
            <Button title ="Send 2" />
            <Button title ="Send 3" />
            <Button title ="Hello World" />
        </div>
    )
  }
  
  
  
export default App */}